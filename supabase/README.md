# Supabase Setup

1. Open your Supabase project SQL editor.
2. Run `supabase/schema.sql`.
3. Create at least one user in Supabase Auth for admin login.

## Notes

- This project does not need a separate custom backend server.
- For production, tighten the job write policies so only admin users can create, update, or delete jobs.
